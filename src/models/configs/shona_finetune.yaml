batch_size: 8
grad_acc_steps: 1
learning_rate: 3.0e-5
weight_decay: 0.002
warmup_steps: 100
lr_decay: linear
decoder_loss_weight: 0.5
max_grad_norm: 1.3
log_every: 10
val_every: 100
save_every: 250
gen_every: 250
num_workers: 2
early_stopping_patience: 4
early_stopping_min_delta: 0.0
